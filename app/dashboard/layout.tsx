
const DashboardLayout = ({children} : {children:React.ReactNode}) => {
  return (
    <div>
      <nav className="bg-gray-200 shadow-sm h-10">
      DashboardLayout...
      </nav>

      {children}
    </div>
  )
}

export default DashboardLayout
